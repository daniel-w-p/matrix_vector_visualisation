# Prompt 15 — Django Integration and Release

**Recommended mode:** Code

```text
Read AGENTS.md, README.md, docs/deployment/django-hosting.md, and docs/backend-plan.md.

Finish the initial Django integration for deployment.

Requirements:
- make sure the frontend production build can be served by Django
- document the development workflow and production workflow clearly
- keep the backend thin
- add minimal backend smoke verification if missing
- update docs to reflect the real repo state

Constraints:
- do not add unrelated backend features
- do not move product logic to the server
- keep deployment assumptions explicit

Verification:
- run the relevant build/check commands
- report the exact results
- list any remaining manual deployment steps
```
