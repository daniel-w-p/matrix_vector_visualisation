# Prompt 05 — SVG Scene Engine

**Recommended mode:** Code

```text
Read AGENTS.md, ARCHITECTURE.md, docs/modules/01-vector-2d.md, docs/modules/02-dot-product.md, and docs/modules/03-matrix-2d-determinant.md.

Implement the reusable 2D SVG scene foundation in `frontend/src/scene2d/`.

Scope:
- axes
- grid
- vector arrow
- label anchor
- angle arc
- filled polygon / area highlight
- draggable point handle
- coordinate conversion helpers if needed

Constraints:
- keep the pieces reusable
- do not hardcode module-specific behavior into the shared scene layer
- keep visuals readable and educational, not decorative
- add tests for non-trivial helper logic

Add a lightweight playground or story-like internal page if useful for manual verification, but do not build full modules yet.

Verification:
- run tests and build
- report what was verified manually
```
