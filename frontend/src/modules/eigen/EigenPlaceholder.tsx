import { PlaceholderScreen } from '../shared/PlaceholderScreen'

export function EigenPlaceholder() {
  return (
    <PlaceholderScreen
      heading="Eigen Module"
      description="Placeholder scene for eigenvectors, eigenvalues, and Av = lambda v checks."
    />
  )
}
